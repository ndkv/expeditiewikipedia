module.exports = Expedition;

var Expedition = function(path) {
	//load config file

	var historicalMaps = [];
	var chapters = [];			//if available show chapters, if not show POIs instead
	var measurements;			//if available
	var route;					//vector file of the route

	var introduction;			//path to introduction text
	var blurp;					//blurp text
};